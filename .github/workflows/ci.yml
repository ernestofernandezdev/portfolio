name: Build and Deploy Docker Image to Docker Hub

on:
  push:
    branches:
      - master  # Puedes cambiar esto a tu rama principal si es diferente

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check out repository
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Cache Docker layers
      uses: actions/cache@v2
      with:
        path: /tmp/.buildx-cache
        key: ${{ runner.os }}-buildx-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-buildx-

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}  # Tu nombre de usuario de Docker Hub
        password: ${{ secrets.DOCKER_PASSWORD }}  # Tu contraseña o token de Docker Hub

    - name: Build Docker image
      run: |
        docker build -t ernestofernandezdev/dev-repo:${{ github.sha }} .

    - name: Push Docker image to Docker Hub
      run: |
        docker push ernestofernandezdev/dev-repo:${{ github.sha }}

    - name: Logout from Docker Hub
      run: |
        docker logout
